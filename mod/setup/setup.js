module.exports = function() {
    log.debug('setup', 'setting up the game')

    // mod to hold text
    console.dir(_)
    _.mod('overlay.mod')

    // add a mod layer to show statistics
    //_.mod('fix/stat')
}
